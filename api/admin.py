from django.contrib import admin

# Note: These are mongoengine models, not Django ORM models
# Django admin integration with mongoengine is limited.
# For data management, use MongoDB Compass directly instead of Django admin.
# The models are managed through REST API endpoints.

